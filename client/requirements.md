## Packages
framer-motion | Page transitions and sleek animations
recharts | Data visualization for response times and uptime
lucide-react | Beautiful icons for the UI
date-fns | Date formatting for logs and charts
clsx | Class name utility
tailwind-merge | Class name utility

## Notes
Authentication uses JWT stored in localStorage.
API requests need `Authorization: Bearer <token>` header.
Dashboard needs polling for real-time updates.
